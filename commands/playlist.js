const { execute } = require("./play");

module.exports = {
    name: "playlist",
    description: "plays playlists on spotify",
    async execute( client, message, args, cmd, Discord )
    {
        console.log("Playlist");
    }

}